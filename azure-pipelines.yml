# ASP.NET Core
# Build and test ASP.NET Core projects targeting .NET Core.
# Add steps that run tests, create a NuGet package, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/dotnet-core

trigger:
- master

pool:  
  vmImage: windows-latest

variables:
  buildConfiguration: 'Release'

steps:
- script: dotnet build --configuration $(buildConfiguration)
  displayName: 'dotnet build $(buildConfiguration)'

# - script: |
#     echo "Listing files..."
#     ls -R
#   displayName: 'Show folder structure'

# - task: Docker@2
#   inputs:
#     containerRegistry: 'DockerConnectionNew'
#     repository: 'ambarajv/DotNetCoreWebApiNew'
#     command: 'buildAndPush'
#     Dockerfile: '$(Build.SourcesDirectory)/DotNetCoreWebApiNew/Dockerfile'
#     buildContext: '$(Build.SourcesDirectory)/DotNetCoreWebApiNew'